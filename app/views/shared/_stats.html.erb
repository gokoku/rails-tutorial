<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <stfong id="following" class="stat">
      <%= @user.following.count %>
    </stfong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <stfong id="followers" class="stat">
      <%= @user.followers.count%>
    </stfong>
    followers
  </a>
</div>